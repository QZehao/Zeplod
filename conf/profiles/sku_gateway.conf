# =============================================================================
# SKU 示例：网关型产品（叠加于 standard + scale）
# =============================================================================
#
# 构建：
#   west build -b <board> . -- \
#     -DEXTRA_CONF_FILE="conf/features/scale.conf;conf/profiles/sku_gateway.conf"
#
# =============================================================================

CONFIG_PROVISIONING_MODULE=y
CONFIG_PROVISIONING_DEVICE_ID="zeplod-gw-sku-01"

CONFIG_FEATURE_GATE_LICENSE="zeplod-oem-gateway"
CONFIG_FEATURE_GATE_BOOT_LICENSE="zeplod-oem-gateway"

CONFIG_CONNECTIVITY_MODULE=y
