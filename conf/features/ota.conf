# OTA module feature overlay (Phase 1 — null transport for native_sim tests)
# Requires CONFIG_EVENT_MAX_TYPES > 51 (incompatible with tiny profile EVENT_MAX_TYPES=8)
CONFIG_OTA_MODULE=y
CONFIG_OTA_NULL_TRANSPORT_BUF_SIZE=4096
CONFIG_MODULE_MANAGER=y
CONFIG_EVENT_SYSTEM=y
