# Optional app-level overrides for framework scripts (copy to app repo root as zephyr_app.env).
# Scripts auto-detect layout when the app root CMakeLists.txt contains add_subdirectory(framework).
#
# APP_PRJ_CONF=gateway_prj.conf
# QEMU_CONF=framework/prj.conf;gateway_prj.conf;framework/prj_qemu.conf
# ZEPHYR_EXTRA_MODULES=modules/zephyr_gateway
